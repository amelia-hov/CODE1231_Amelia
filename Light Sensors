import time 
import board 
import neopixel

pixels = neopixel.Neopixel(board.NEOPIXEL, 10, brightness=0.3)

while True:
    pixels.fill((255,0,0))
    time.sleep(1)
    pixels.fill((0,255,0))
    time.sleep(1)

